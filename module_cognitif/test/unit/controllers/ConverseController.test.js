"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/ConverseController');

describe('controllers:ConverseController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
